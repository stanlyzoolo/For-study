# Примечание: бинарный поиск по списку работает только в том случае, если список отсортирован.
# Функция binary_search получает отсортированный массив и позицию( индекс), в которой он был найден.
# Бинарный поиск выполняется за логарифмическое время (операция обратная возведению в степень). 
def binary_search (list, item):
	# Определение границ поиска в переменных low и high.
	low = 0
	high = len(list)-1
	# Задаем цикл поиска необходимого элемента item.
	while low < high:
		mid = (low + high)
		# Тело цикла.
		guess=list[mid]
		# Если предполагаемое значение найдено.
		if guess == item:
			return mid
		# Много
		if guess > item:
			high = mid - 1
		# Мало
		else:
			low = mid - 1
	return None
# Протестируем функцию.

my_list = [1, 3, 5, 7 , 9]

print (binary_search(my_list, 3))
