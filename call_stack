
# Стек - простая структура данных. Принцип работы - "последним пришел - первым вышел".

name = input ()

def greet (name):
  print ("Hello, " + name + "!")
  greet2 (name)                                   # Вызов функции greet2 и выполнение ее назначения
  print ("getting ready to say bye...")           # Возврашение к функции greet и выполнение команды print ()
  bye()                                           # Вызов функции greet2 из стека вызовов
                                                  # Т.к. задач из стека вызовов и 
                                                  # тела функции greet не осталось - работа завершена.
                                                  
# При вызове функции greet2 функция greet еще не была завершена.

# Важно! Когда функция вызывается из другой функции,
# вызывающая функция приостанавливается в частично завершенном состоянии.
  
def greet2(name):
  print ("How are you, ?" + name + "?")

def (bye):
  print ("Ok, bye!")

print (greet(name))

# Стек вызовов - это стек, в котором сохранялись переменные разных функций.
